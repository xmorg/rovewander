
class CharacterGenerator:
    def __init__(self, scr, characters, tiles):
        self.windowname = "chargen"
        self.scr = scr
        self.characters = res
        self.tiles = tiles
:q
#for i in range(0, 20):
        #----self.chartileset.append(self.worldtiles.tiles.subsurface(r))
    def draw(self): #user self.scr to blit actors to create chargen
        i = 0
